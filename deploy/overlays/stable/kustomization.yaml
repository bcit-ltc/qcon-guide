apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: qcon

resources:
  - ../../base

commonLabels:
  environment: stable

commonAnnotations:
  version: stable # {"$imagepolicy": "flux-system:qcon-guide:tag"}
  tab: ${GIT_TAG:=stable}

configMapGenerator:
  - name: elasticapm-config
    literals:
      - ELASTIC_APM_SERVICE_NAME=qcon-guide-stable
      - ELASTIC_APM_SERVER_URL=https://bcit-ltc.apm.westus2.azure.elastic-cloud.com

patches:
  - target:
      kind: Deployment
    patch: |-
      - op: add
        path: /spec/template/spec/containers/0/envFrom
        value: [{"configMapRef": {"name":"elasticapm-config"}}]

images:
  - name: "qcon-guide"
    newName: "registry.ltc.bcit.ca/web-apps/qcon/qcon-guide"
    newTag: "stable" # {"$imagepolicy": "flux-system:qcon-guide:tag"}
