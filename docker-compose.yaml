name: qcon-guide
services:
  app:
    image: qcon-guide
    command: ["serve", "--dev-addr=0.0.0.0:8080"]
    build:
      target: build
    environment:
      - DEBUG=true
      - CI=false
    volumes:
    - .:/app
    ports:
      - "8080:8080"